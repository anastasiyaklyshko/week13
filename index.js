function showMessage () {
let form = prompt ('Как тебя зовут?');
alert (`Привет, ${form}!`);
}

function calcAdd () {
let a = prompt ('Первое число:');
let b = prompt ('Второе число:');
alert (Number(a)+Number(b));
}

function calcSub () {
let a = prompt ('Первое число:');
let b = prompt ('Второе число:');
alert (Number(a)-Number(b));
}

function calcMulti () {
let a = prompt ('Первое число:');
let b = prompt ('Второе число:');
alert (Number(a)*Number(b));
}

function calcDiv () {
let a = prompt ('Первое число:');
let b = prompt ('Второе число:');
alert (Number(a)/Number(b));
}